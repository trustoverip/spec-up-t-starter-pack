const setupCompleteMessage = `Spec-Up-T setup complete.\n\nNow cd into the directory that you just created and run "npm install" to install dependencies:\n\ncd ${dirName}\n\nnpm install`;

module.exports = {
    setupCompleteMessage
};